# Shared CLI packages for Debian/Ubuntu — installed on both hosts (setup-debian.sh)
# and the agent sandbox guest (tools/sandbox/Containerfile). One package per line, '#' comments.
# Host-only extras (openssh-server, fonts, gh's apt repo, GUI apps) are added by setup-debian.sh.
bash-completion
bat
build-essential
ca-certificates
curl
fd-find
fzf
git
git-delta
gnupg
htop
ncdu
openssh-client
procps
ripgrep
tmux
wget
zoxide
