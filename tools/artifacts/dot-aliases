# fastfetch greeting function
fastfetch_greeting() {
  shopt -q login_shell && [[ -z "$TMUX" ]] && {
    logos=("gnu") # "aperture"
    selected_logo=${logos[$RANDOM % ${#logos[@]}]}
    fastfetch --logo $selected_logo
  }
}

# update more easily
alias sup="sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y"
alias pup="sudo pacman -Syu"

# some more ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# Add an "alert" alias for long running commands.  Use like so:
#   sleep 10; alert
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'

# Navigation aliases
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias .....='cd ../../../..'

# Better grep - use ripgrep if available, otherwise enhanced grep
if command -v rg &>/dev/null; then
  alias grep='rg --smart-case'
  alias rg='rg --smart-case'
  alias rgf='rg --files-with-matches'
  alias rgi='rg --no-ignore'
else
  alias grep='grep --color=auto --exclude-dir={.git,.svn,.hg}'
fi

# Use fd if available for finding files
if command -v fd &>/dev/null; then
  alias fd='fd --hidden --follow'
  alias fdi='fd --no-ignore --hidden --follow'
elif command -v fdfind &>/dev/null; then
  alias fd='fdfind --hidden --follow'
  alias fdi='fdfind --no-ignore --hidden --follow'
fi

# Better cat with syntax highlighting
if command -v bat &>/dev/null; then
  alias cat='bat --style=plain'
  alias batl='bat --style=full'
elif command -v batcat &>/dev/null; then
  alias cat='batcat --style=plain'
  alias batl='batcat --style=full'
  alias bat='batcat'
fi

# Better disk usage
if command -v ncdu &>/dev/null; then
  alias du='ncdu --color dark'
fi

# Git delta for better diffs
if command -v delta &>/dev/null; then
  export GIT_PAGER='delta'
fi

# FZF-powered aliases if available
if command -v fzf &>/dev/null; then
  # History search with fzf
  alias h='history | fzf --tac'

  # cd to directory using fzf
  fcd() {
    local dir
    dir=$(find ${1:-.} -type d 2>/dev/null | fzf +m) && cd "$dir"
  }

  # Open file in editor using fzf
  fe() {
    local files
    IFS=$'\n' files=($(fzf --query="$1" --multi --select-1 --exit-0))
    [[ -n "$files" ]] && ${EDITOR:-vim} "${files[@]}"
  }

  # Kill process using fzf
  fkill() {
    local pid
    pid=$(ps -ef | sed 1d | fzf -m | awk '{print $2}')
    if [ "x$pid" != "x" ]; then
      echo $pid | xargs kill -${1:-9}
    fi
  }
else
  # Fallback history search
  alias h='history | grep'
fi

# Extract various archive formats
extract() {
  if [ -f "$1" ]; then
    case "$1" in
    *.tar.bz2) tar xjf "$1" ;;
    *.tar.gz) tar xzf "$1" ;;
    *.bz2) bunzip2 "$1" ;;
    *.rar) unrar e "$1" ;;
    *.gz) gunzip "$1" ;;
    *.tar) tar xf "$1" ;;
    *.tbz2) tar xjf "$1" ;;
    *.tgz) tar xzf "$1" ;;
    *.zip) unzip "$1" ;;
    *.Z) uncompress "$1" ;;
    *.7z) 7z x "$1" ;;
    *) echo "'$1' cannot be extracted via extract()" ;;
    esac
  else
    echo "'$1' is not a valid file"
  fi
}

yank() {
  local content
  if [[ -f "$1" ]]; then
    content=$(cat "$1")
  else
    content=$(cat)
  fi

  if [[ "$OSTYPE" == "darwin"* ]] && [[ -z "$TMUX" ]]; then
    printf "%s" "$content" | pbcopy
  else
    local b64=$(printf "%s" "$content" | base64 | tr -d '\n')
    echo -en "\033Ptmux;\033\033]52;c;${b64}\007\033\\"
  fi
}

# Claude Flow aliases with high performance settings
alias cf-init="npx claude-flow@alpha init"

# Claude Flow with high performance settings - takes quoted instruction
cf() {
  if [ $# -eq 0 ]; then
    echo "Usage: cf \"instruction\""
    echo "Example: cf \"Build a REST API with authentication\""
    return 1
  fi

  npx claude-flow@alpha hive-mind spawn "$1" \
    --auto-spawn \
    --claude \
    --max-workers 20 \
    --auto-scale \
    --monitor \
    --memory-size 200 \
    --namespace flow
}

# Swarm alias with high performance settings - takes quoted instruction
sw() {
  if [ $# -eq 0 ]; then
    echo "Usage: sw \"instruction\""
    echo "Example: sw \"Build a REST API with authentication\""
    return 1
  fi

  npx claude-flow@alpha swarm "$1" \
    --parallel \
    --max-agents 20 \
    --mode hybrid \
    --claude
}

alias avante='nvim -c "lua vim.defer_fn(function()require(\"avante.api\").zen_mode()end, 100)"'

alias dan='claude --dangerously-skip-permissions'

# Happier (Claude Code UI wrapper) - preview channel since no stable releases yet
alias install-happier-preview='curl -fsSL https://happier.dev/install-preview | bash'
